# CLAUDE.md

本仓库是 Claude Code / Codex 一键安装脚本的唯一真源，被 `Liang-HZ/ai-cli-installers-page` 仓库的 CI 消费（拉取脚本、注入 SHA256 到 install.liangai.org）。

改动脚本文件名/路径前，务必先看 `docs/RUNBOOK.md` 了解与下游仓库的消费契约，避免下游 CI 报错。

详见：[docs/RUNBOOK.md](docs/RUNBOOK.md)
